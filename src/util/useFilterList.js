import restaurantList from "./MockData.js";


const useFilterList =()=>{


// const useFilterList = (initialList) => {
//   const [filteredList, setFilteredList] = useState(initialList);

//   const filterByKeyword = (list, keyword) => {
//     const filteredData = list.filter((data) =>
//       data.info.name.toLowerCase().includes(keyword.toLowerCase())
//     );
//     setFilteredList(filteredData);
//   };

//   const filterByRating = (list, minRating) => {
//     const filteredData = list.filter((data) => data.info.avgRating > minRating);
//     setFilteredList(filteredData);
//   };

//   const filterByDeliveryTime = (list, maxDeliveryTime) => {
//     const filteredData = list.filter(
//       (data) => data.info.sla.deliveryTime < maxDeliveryTime
//     );
//     setFilteredList(filteredData);
//   };

//   return {
//     filteredList,
//     filterByKeyword,
//     filterByRating,
//     filterByDeliveryTime,
//   };
// };





    
}

export default useFilterList;