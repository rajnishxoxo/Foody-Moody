
const RestroMenuCategory =(props)=>{
    console.log(props)

    const {title} = props.data

    console.log(title)
    
    return(
        <div>
            {/* <h1>{title}</h1> */}
        </div>
    )
}

export default RestroMenuCategory;